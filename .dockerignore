# Docker 빌드 컨텍스트에서 제외 — 이미지 크기/빌드 시간 절약

# Frontend (백엔드 이미지엔 불필요)
wep/

# Git 메타
.git/
.gitignore
.github/

# Python venv / 캐시
**/venvs/
**/venv/
**/__pycache__/
**/*.pyc

# 노트북 / 데이터셋 / 학습 산출물 중 불필요한 것
*.ipynb
*.log
backend/uploads/
backend/multiclass_wrong_samples.png
backend/diagnosis.py.bak

# 옛 학습 체크포인트 (현재 백엔드는 100%_augw + _mp 만 로드 — 나머진 불필요하지만
# 안전하게 전체 checkpoint2/ 는 포함. 더 가볍게 하려면 아래 두 라인 활성화:
# checkpoint2/class/100%/
# checkpoint2/regression/100%/1,2,3/

# 백업 / 한글 이름 폴더
backend - 복사본/
*복사본*
AI_Skin_Analyst/

# IDE
.vscode/
.idea/
.DS_Store
Thumbs.db

# 환경변수 (시크릿 절대 제외)
.env
**/.env
!.env.example
!**/.env.example
